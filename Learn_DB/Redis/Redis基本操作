字符串(String)相关操作
	1. set --设置值
	2. get --获取值
	3. mset --设置多个键值对
	4. mget --获取多个键值对
	5. append --添加字符串
	6. del --删除
	7. incr/decr --增加/减少 1
	8. EX -- 过一段时间删除

列表(list)相关操作
	1. lpush/rpush -- 从左/右插入数据
	2. lrange --  获取指定长度的数据
	3. ltrim -- 截取一定长度的数据
	4. lpop/rpop -- 祎楚最左/最右元素并返回
	5. lpushx/rpushx -- key存在的是有才插入数据, 不存在时不做任何处理

集合(Set)相关操作
    1. sadd/srem --添加/删除元素
    2. sismember --判断是否为set的一个元素
    3. smembers --返回该集合的所有成员
    4. sdiff --返回一个集合与其他集合的差异
    5. sinter --返回几个集合的交集
    6. sunion -- 返回几个集合的并集

散列(Hash)相关操作
	1. hset/hget --设置/获取散列值
	2. hmset/hmget --设置/获取多对散列值
	3. hsetnx --如果散列已经存在, 则不设置
	4. hkeys/hvals --返回所有Keys/Values
	5. hlen --返回散列包含域(field)的数量
	6. hdel --删除所有散列指定的域(field)
	7. hexists --判断是否存在

